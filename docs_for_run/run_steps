1. Initialization
  - Log into Langfuse
  - Run VLLM
```
vllm serve 'models/Qwen/Qwen2.5-Coder-32B.gguf
```
  
  
  # Get all Wii sales data
  query_sales(where_column="Platform", where_value="Wii")
  
  # Get just platform and global sales, sorted by sales
  query_sales(columns="Platform,Global_Sales", order_by="Global_Sales DESC", limit=10)
  
  # Get sales data for a specific year
  query_sales(where_column="Year", where_value="2009")
  
  # Get review data for PS4 users
  query_reviews(columns="age,education,review", where_column="platform", where_value="PS4", limit=5)
  
  # Get highest spending score customers
  query_reviews(columns="age,spending_score,loyalty_points", order_by="spending_score DESC", limit=10)



Syst prompts comes out similar to this:
```
  - retrieve_metadata: Search the dataset metadata for relevant information
      Takes inputs: { "query": { "type": "string", "description": "What to search for in the metadata" }, "k": { "type": "integer", "description": "Number of results to return (default: 3)" } }
      Returns an output of type: string
```